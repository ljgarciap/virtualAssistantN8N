# CONFIGURACIÓN BÁSICA
N8N_PORT=5678
OLLAMA_PORT=11434
N8N_PROTOCOL=https

# MODELOS
OLLAMA_MODEL=mistral
WHISPER_MODEL=small  # small|medium|large-v2

# RUTAS DE PERSISTENCIA (absolutas o relativas)
N8N_DATA=./n8n_data
OLLAMA_DATA=./ollama_data

# SSL (generar con: openssl req -x509 -newkey rsa:4096 -nodes -keyout certs/key.pem -out certs/cert.pem -days 365)
SSL_CERT=./certs/cert.pem
SSL_KEY=./certs/key.pem